// Boolean Values and Ternary Operator

// Boolean:
// Boolean ek data type hai jo sirf do values hold karta hai: true ya false.

// Ternary Operator:
// Ternary operator ek shorthand conditional operator hai. Format hota hai: condition ? expressionIfTrue : expressionIfFalse;

let age = 20;
let message = age >= 18 ? "You can vote" : "You are too young";

console.log(message);
